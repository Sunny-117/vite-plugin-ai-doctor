# Playground 快速开始

## 1. 安装依赖

在项目根目录运行：

```bash
pnpm install
```

这会在根目录和 playground 目录都安装依赖。

## 2. 配置 LLM 模型

确保在 `../src/core/llm.ts` 中配置了你的本地大模型（如 Ollama）。

## 3. 测试正常构建

```bash
cd playground
pnpm build
```

应该能正常构建成功。

## 4. 测试错误诊断

### 方法 1: 导入不存在的模块

编辑 `src/main.ts`，添加：

```ts
import './test-error'  // 取消注释 test-error.ts 中的错误代码
```

编辑 `src/test-error.ts`，取消注释：

```ts
import { something } from './non-existent-file'
```

然后运行：

```bash
pnpm build
```

### 方法 2: TypeScript 类型错误

编辑 `src/error-example.ts`，取消注释：

```ts
const num: number = 'this is a string'
```

在 `src/main.ts` 中导入：

```ts
import './error-example'
```

然后运行：

```bash
pnpm build
```

## 5. 观察插件行为

当构建失败时，你应该看到：

1. 🚨 红色 Banner："智能报错诊断系统启动"
2. 🤖 黄色提示："AI 正在分析构建错误，请稍候..."
3. 💡 AI 诊断建议（打字机效果输出）

## 故障排除

- 如果 AI 调用失败，检查 `../src/core/llm.ts` 中的模型配置
- 确保本地 LLM 服务已启动（如 `ollama serve`）
- 检查网络连接

